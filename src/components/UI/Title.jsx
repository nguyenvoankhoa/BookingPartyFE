import React from "react";

const Title = ({ name }) => {
  return (
    <div className="mt-5 mb-5 dot-line text-center">
      <span className="fw-700">{name}</span>
    </div>
  );
};

export default Title;
